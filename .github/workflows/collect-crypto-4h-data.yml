name: Collect Crypto 4H Data

on:
  schedule:
    - cron: '0 8 * * *'   # 08:00 UTC = 16:00 Taiwan time (daily)
  workflow_dispatch:        # Manual trigger

permissions:
  contents: write
  models: read

jobs:
  collect:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install package
        run: pip install -e .

      - name: Collect 4H crypto data
        run: python scripts/collect_4h_to_db.py

      - name: Send Telegram notification
        env:
          GITHUB_TOKEN: ${{ github.token }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python scripts/send_4h_tg_notification.py

      - name: Commit and push database
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/history.db
          git diff --staged --quiet && echo "No changes to commit" && exit 0
          git commit -m "data: daily 4H crypto snapshot $(date -u +'%Y-%m-%dT%H:%M:%SZ')"
          git pull --rebase
          git push
